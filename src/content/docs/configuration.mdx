---
title: Configuration
description: Configuring your Gestalt workspace
order: 2
---

# Configuration

Customize your Gestalt workspace to match your workflow.

## Theme

Gestalt uses a custom dark palette inspired by Catppuccin. All colors are defined as CSS custom properties in `global.css`:

```css
:root {
  --ctp-crust: #0e0c13;     /* darkest bg */
  --ctp-mantle: #1b1624;    /* panels */
  --ctp-base: #221e2f;      /* main bg */
  --ctp-lavender: #ae9ad6;  /* headings */
  --ctp-blue: #84a5cb;      /* links, labels */
  --ctp-peach: #e7a988;     /* inline code */
  --ctp-green: #add692;     /* success */
  --ctp-accent: #ea31b5;    /* primary accent */
}
```

## Desktop Icons

Icons can be rearranged by dragging them. Positions are saved to `localStorage` automatically.

To reset all icon positions, **right-click** on the desktop and select **Clear cache**.

## Window Behavior

| Action | Behavior |
|--------|----------|
| Click icon | Opens app window |
| Drag title bar | Moves window |
| Drag bottom-right corner | Resizes window |
| `─` button | Minimizes to taskbar |
| `□` button | Toggles maximize |
| `✕` button | Closes window |

## Adding Content

Create `.mdx` files in `src/content/docs/` with frontmatter:

```yaml
---
title: My Page
description: A brief summary
order: 3
---
```

The content will automatically appear in the Docs window on the desktop.
